const fs = require('fs'); const files = ['src/pages/Tours.tsx', 'src/pages/TourDetail.tsx', 'src/pages/Contacts.tsx', 'src/pages/About.tsx']; files.forEach(f => { let content = fs.readFileSync(f, 'utf8'); content = content.replace(/\s*(initial|animate|whileInView|viewport|transition)=\{\{([^}]+)\}\}/g, ''); content = content.replace(/<(motion\.[a-z0-9]+)/g, (m, tag) => '<' + tag.split('.')[1]); content = content.replace(/<\/(motion\.[a-z0-9]+)>/g, (m, tag) => '</' + tag.split('.')[1] + '>'); fs.writeFileSync(f, content); });
